$(document).ready(function(){
  $('.slider-box').slick({
    dots: true,
    autoplay: true,
    autoplaySpeed: 3000,
    arrows: false,
    accessebillity: true
  });

  $('.container').mixItUp();

});